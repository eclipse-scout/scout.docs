==== NLS Entry Wizard
:doctype: article
:revnumber: {project-version}
include::_initDoc.adoc[]

//fallback for safe mode == secure:
ifndef::imgsdir[:imgsdir: ../../../../imgs]
ifndef::codedir[:codedir: ../../../../../code]
ifndef::mdledir[:mdledir: ../../../../adoc]
:experimental:


The NLS Entry wizard can be used to create or to modify a text entry that will be displayed to the user and the needs to be localized.
It can be opened from:

* <<sdk_editor_nls.adoc#, The NLS Editor>> by selecting "New Entry" or "Modify Entry".
* <<sdk_editor_java.adoc#, The Java Editor>> by using the proposal editor inside the string parameter of the `TEXTS.get("")` code.

image::{imgsdir}/sdk_wizard_nls_entry.png[]

The [field]_Key Name_ field holds the text key that is used to access translated text.

In the tabs below the key name field, the text translations for the registered languages can be provided. 
Make sure to at least provide a text in the [element]_default_ tab. 

With the [element]_copy key to the clipboard_ you can copy the key name to the clipboard and paste it later in your code.

=======
include::{mdledir}/_footer.adoc[]
